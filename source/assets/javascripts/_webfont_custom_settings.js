window.WebFontConfig = {
  custom: {
    families: ['Merriweather:n4,n7']
  },
  // This can be set very high because the text content of the page is still
  // visible before the web fonts load.
  timeout: 60 * 1000
};
