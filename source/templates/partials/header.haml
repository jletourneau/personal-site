%header
  .container
    - if page_classes != 'index'
      %nav{ role: 'navigation' }
        %ul
          %li= link_to 'Home', '/index.html'
          = yield if block_given?
    %h1= current_page.data.title
